<%= form_for(@coffee_attempt) do |f| %>
  <% if @coffee_attempt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coffee_attempt.errors.count, "error") %> prohibited this coffee_attempt from being saved:</h2>

      <ul>
      <% @coffee_attempt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
