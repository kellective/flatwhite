!!!
= html_tag :class => "no-js"
= render "head"
%body{:class => controller.controller_name}
  = render "chromeframe"
  = render "header"
  

  .container
    =render :partial => "application/flash_messages", :locals => {:flash => flash}

    %h1 FlatWhite Fortunes
    = yield
  = render "footer"
  = render "javascripts"